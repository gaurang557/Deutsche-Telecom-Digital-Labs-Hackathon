[pytest]
# asyncio_mode=auto lets `async def test_*` run without per-test decorators.
asyncio_mode = auto
# Make the repo root importable so `import windows_agent` works.
pythonpath = .
testpaths = tests
