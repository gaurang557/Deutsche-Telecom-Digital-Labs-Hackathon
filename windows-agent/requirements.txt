# Milestone 0 — execution contract only. Kept intentionally minimal;
# heavier dependencies (PDF/spreadsheet/desktop/LLM/voice) arrive with the
# milestones that need them.
pydantic>=2.6
pytest>=8.0
pytest-asyncio>=0.23

# Milestone 3 — read-only PDF executor. PyMuPDF (imported as `fitz`) is the
# structured PDF API used by `executors/pdf_ops.py` (see ARCHITECTURE §3/§9).
PyMuPDF>=1.24

# Milestone 4 — spreadsheet executor. openpyxl is the structured .xlsx API used
# by `executors/spreadsheet_ops.py` (see ARCHITECTURE §3/§9).
openpyxl>=3.1

# Milestone 6 — document (Word) executor. python-docx (imported as `docx`) is the
# structured .docx API used by `executors/document_ops.py` (see ARCHITECTURE
# §3/§9). NOTE: the pip package is `python-docx`; the import name is `docx`.
python-docx>=1.1

# Milestone 7 — presentation (PowerPoint) executor. python-pptx (imported as
# `pptx`) is the structured .pptx API used by `executors/presentation_ops.py`
# (see ARCHITECTURE §3/§9). NOTE: the pip package is `python-pptx`; the import
# name is `pptx`.
python-pptx>=1.0
